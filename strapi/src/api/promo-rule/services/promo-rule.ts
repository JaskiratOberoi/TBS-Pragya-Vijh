/**
 * Core service (generated). Do not remove — used by the core controller.
 */
import { factories } from "@strapi/strapi";

export default factories.createCoreService("api::promo-rule.promo-rule");
