/**
 * Core REST controller (generated). Do not remove — required by routes/invoice.js.
 */
import { factories } from "@strapi/strapi";

export default factories.createCoreController("api::invoice.invoice");
