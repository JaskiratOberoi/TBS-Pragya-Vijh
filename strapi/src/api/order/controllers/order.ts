/**
 * Core REST controller (generated). Do not remove — required by routes/order.js.
 */
import { factories } from "@strapi/strapi";

export default factories.createCoreController("api::order.order");
