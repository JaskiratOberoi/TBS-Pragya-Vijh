/**
 * Core REST controller (generated). Do not remove — required by routes/blocked-slot.js.
 */
import { factories } from "@strapi/strapi";

export default factories.createCoreController("api::blocked-slot.blocked-slot");
