export default {
  /**
   * Register lifecycle hooks or custom logic before app loads.
   */
  register(/* { strapi } */) {},

  /**
   * Bootstrap — runs before app starts.
   */
  bootstrap(/* { strapi } */) {},
};
